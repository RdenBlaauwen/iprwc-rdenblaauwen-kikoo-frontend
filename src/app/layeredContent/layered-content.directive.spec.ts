import { LayeredContentDirective } from './layered-content.directive';

describe('LayeredContentDirective', () => {
  it('should create an instance', () => {
    const directive = new LayeredContentDirective();
    expect(directive).toBeTruthy();
  });
});
